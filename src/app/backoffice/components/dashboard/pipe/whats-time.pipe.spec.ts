import { WhatsTimePipe } from './whats-time.pipe';

describe('WhatsTimePipe', () => {
  it('create an instance', () => {
    const pipe = new WhatsTimePipe();
    expect(pipe).toBeTruthy();
  });
});
